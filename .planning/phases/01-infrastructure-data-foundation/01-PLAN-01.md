---
phase: 1
plan: 1
title: "Project skeleton, Docker Compose, and core configuration"
wave: 1
depends_on: []
requirements_addressed: [INFRA-01, INFRA-03]
files_modified:
  - pyproject.toml
  - Dockerfile
  - Dockerfile.gpu
  - docker-compose.yml
  - .env.example
  - src/poseidon/__init__.py
  - src/poseidon/main.py
  - src/poseidon/core/__init__.py
  - src/poseidon/core/config.py
  - src/poseidon/api/__init__.py
  - src/poseidon/api/auth.py
  - src/poseidon/api/health.py
  - src/poseidon/data/__init__.py
  - src/poseidon/data/fetchers/__init__.py
  - src/poseidon/models/__init__.py
  - src/poseidon/workers/__init__.py
  - src/poseidon/strategies/__init__.py
  - src/poseidon/signals/__init__.py
  - src/poseidon/backtest/__init__.py
  - src/poseidon/risk/__init__.py
  - config/symbols.yaml
  - alembic.ini
  - alembic/env.py
  - alembic/versions/.gitkeep
  - tests/__init__.py
  - tests/conftest.py
autonomous: true
estimated_tasks: 10
---

# Plan 1: Project skeleton, Docker Compose, and core configuration

<objective>
Create the complete project skeleton, Docker Compose with all 6 services, core configuration module, API key authentication middleware, health endpoint, and the Dockerfile pair -- establishing the foundation every other plan depends on.
</objective>

<must_haves>
- `pyproject.toml` exists with all Phase 1 dependencies listed
- `docker-compose.yml` defines exactly 6 services: api, beat, gpu-worker, cpu-worker, redis, postgres
- `Dockerfile` builds the base image for api, beat, and cpu-worker
- `Dockerfile.gpu` exists as a stub for gpu-worker
- `src/poseidon/core/config.py` defines a `Settings` class using pydantic-settings with `env_prefix = "POSEIDON_"`
- `src/poseidon/api/auth.py` implements X-API-Key header validation
- `src/poseidon/api/health.py` provides a `/health` GET endpoint that returns `{"status": "ok"}`
- `src/poseidon/main.py` creates a FastAPI app and mounts health (no auth) and secured routers (with auth)
- `alembic.ini` and `alembic/env.py` are configured for Poseidon
- `config/symbols.yaml` contains 20+ symbols for equity/crypto markets (tw_stock >= 20, us_stock >= 20, crypto_spot >= 10); tw_futures is an exception with all available major contracts (>= 4) since the Taiwan futures market has a limited number of distinct contracts
- All future-phase module directories exist as empty packages (`strategies/`, `signals/`, `backtest/`, `risk/`)
</must_haves>

<tasks>

<task id="1.1">
<title>Create pyproject.toml with all Phase 1 dependencies</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/triton/pyproject.toml
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/.planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md (section 9: Dependency Version Matrix)
</read_first>
<action>
Create `pyproject.toml` at the project root with these exact contents:

```toml
[project]
name = "poseidon"
version = "0.1.0"
description = "Trading signal platform for OpenClaw"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg2-binary>=2.9",
    "pydantic-settings>=2.0",
    "celery[redis]>=5.4",
    "celery-redbeat>=2.2",
    "redis>=5.0",
    "pandas>=2.2",
    "yfinance>=0.2.40",
    "ccxt>=4.0",
    "requests>=2.31",
    "pyyaml>=6.0",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
gpu = [
    "torch>=2.0",
    "xgboost>=2.0",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "pytest-cov>=5.0",
    "ruff>=0.5",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 120
target-version = "py311"
```
</action>
<acceptance_criteria>
- File exists at `poseidon/pyproject.toml`
- `grep -c 'name = "poseidon"' pyproject.toml` returns `1`
- `grep 'celery-redbeat>=2.2' pyproject.toml` matches
- `grep 'yfinance>=0.2.40' pyproject.toml` matches
- `grep 'ccxt>=4.0' pyproject.toml` matches
- `grep 'where = \["src"\]' pyproject.toml` matches
- `grep 'asyncio_mode = "auto"' pyproject.toml` matches
</acceptance_criteria>
</task>

<task id="1.2">
<title>Create Dockerfile for base image (api, beat, cpu-worker)</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/pyproject.toml (just created in 1.1)
</read_first>
<action>
Create `Dockerfile` at the project root:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Install system dependencies for psycopg2
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libpq-dev && \
    rm -rf /var/lib/apt/lists/*

COPY pyproject.toml .
RUN pip install --no-cache-dir .

COPY src/ src/
COPY config/ config/
COPY alembic/ alembic/
COPY alembic.ini .

# Default command: run the API server
CMD ["uvicorn", "poseidon.main:app", "--host", "0.0.0.0", "--port", "8000"]
```
</action>
<acceptance_criteria>
- File exists at `poseidon/Dockerfile`
- `grep 'FROM python:3.11-slim' Dockerfile` matches
- `grep 'pip install --no-cache-dir' Dockerfile` matches
- `grep 'poseidon.main:app' Dockerfile` matches
- `grep 'COPY src/ src/' Dockerfile` matches
- `grep 'COPY config/ config/' Dockerfile` matches
</acceptance_criteria>
</task>

<task id="1.3">
<title>Create Dockerfile.gpu stub for gpu-worker</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/Dockerfile (just created in 1.2)
</read_first>
<action>
Create `Dockerfile.gpu` at the project root. For Phase 1 this is identical to the base Dockerfile since there is no model training yet. The GPU worker just needs to start and connect to Redis.

```dockerfile
FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libpq-dev && \
    rm -rf /var/lib/apt/lists/*

COPY pyproject.toml .
RUN pip install --no-cache-dir .

COPY src/ src/
COPY config/ config/
COPY alembic/ alembic/
COPY alembic.ini .

# GPU worker command (overridden in docker-compose.yml)
CMD ["celery", "-A", "poseidon.workers.celery_app", "worker", "-Q", "gpu", "-c", "1", "--loglevel=info"]
```
</action>
<acceptance_criteria>
- File exists at `poseidon/Dockerfile.gpu`
- `grep 'FROM python:3.11-slim' Dockerfile.gpu` matches
- `grep 'poseidon.workers.celery_app' Dockerfile.gpu` matches
- `grep -- '-Q gpu' Dockerfile.gpu` matches (note: `--` to prevent flag interpretation)
</acceptance_criteria>
</task>

<task id="1.4">
<title>Create docker-compose.yml with all 6 services</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/triton/docker-compose.yml
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/.planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md (section 5: Docker Compose Structure)
</read_first>
<action>
Create `docker-compose.yml` at the project root with exactly 6 services. Use port 8001 on host (avoid Triton collision). Use `timescale/timescaledb:latest-pg16` for postgres. Include healthchecks for postgres, redis, and api. Use named volumes. Redis must have `--appendonly yes` for RedBeat persistence. The beat, gpu-worker, and cpu-worker services must depend on `api: condition: service_healthy` (not just redis/postgres) to prevent a race condition where workers start before the API finishes running `alembic upgrade head` on first boot.

```yaml
services:
  api:
    build: .
    ports:
      - "8001:8000"
    environment:
      - POSEIDON_DATABASE_URL=postgresql://poseidon:poseidon@postgres:5432/poseidon
      - POSEIDON_REDIS_URL=redis://redis:6379/0
      - POSEIDON_API_KEY=${POSEIDON_API_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  beat:
    build: .
    command: celery -A poseidon.workers.celery_app beat --loglevel=info
    environment:
      - POSEIDON_DATABASE_URL=postgresql://poseidon:poseidon@postgres:5432/poseidon
      - POSEIDON_REDIS_URL=redis://redis:6379/0
    depends_on:
      api:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  gpu-worker:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    command: celery -A poseidon.workers.celery_app worker -Q gpu -c 1 --loglevel=info
    environment:
      - POSEIDON_DATABASE_URL=postgresql://poseidon:poseidon@postgres:5432/poseidon
      - POSEIDON_REDIS_URL=redis://redis:6379/0
    volumes:
      - model-artifacts:/data/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    depends_on:
      api:
        condition: service_healthy
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    restart: unless-stopped

  cpu-worker:
    build: .
    command: celery -A poseidon.workers.celery_app worker -Q cpu -c 4 --loglevel=info
    environment:
      - POSEIDON_DATABASE_URL=postgresql://poseidon:poseidon@postgres:5432/poseidon
      - POSEIDON_REDIS_URL=redis://redis:6379/0
      - POSEIDON_FINMIND_TOKEN=${FINMIND_TOKEN}
    volumes:
      - model-artifacts:/data/models
    depends_on:
      api:
        condition: service_healthy
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    volumes:
      - poseidon-redis:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  postgres:
    image: timescale/timescaledb:latest-pg16
    environment:
      POSTGRES_USER: poseidon
      POSTGRES_PASSWORD: poseidon
      POSTGRES_DB: poseidon
    volumes:
      - poseidon-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U poseidon"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  poseidon-redis:
  poseidon-pgdata:
  model-artifacts:
```
</action>
<acceptance_criteria>
- File exists at `poseidon/docker-compose.yml`
- `grep -c 'image: timescale/timescaledb:latest-pg16' docker-compose.yml` returns `1`
- `grep -c 'image: redis:7-alpine' docker-compose.yml` returns `1`
- `grep -c '"8001:8000"' docker-compose.yml` returns `1`
- `grep -c 'appendonly yes' docker-compose.yml` returns `1`
- `grep -c 'pg_isready -U poseidon' docker-compose.yml` returns `1`
- `grep 'POSEIDON_API_KEY' docker-compose.yml` matches
- `grep 'POSEIDON_DATABASE_URL' docker-compose.yml` matches
- The following 6 service names appear: api, beat, gpu-worker, cpu-worker, redis, postgres
- `grep -c 'restart: unless-stopped' docker-compose.yml` returns `6`
- Beat, gpu-worker, and cpu-worker all depend on `api: condition: service_healthy` to ensure Alembic migrations complete before workers start: `grep -A2 'depends_on' docker-compose.yml | grep -c 'api'` returns at least `3`
</acceptance_criteria>
</task>

<task id="1.5">
<title>Create .env.example with all required environment variables</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/docker-compose.yml (just created in 1.4)
</read_first>
<action>
Create `.env.example` at the project root listing every environment variable the stack needs. Users copy this to `.env` and fill in real values.

```bash
# Poseidon Environment Configuration
# Copy to .env and fill in real values

# API authentication key (required for all secured endpoints)
POSEIDON_API_KEY=change-me-to-a-real-secret

# FinMind API token (register at https://finmindtrade.com)
FINMIND_TOKEN=your-finmind-token-here
```
</action>
<acceptance_criteria>
- File exists at `poseidon/.env.example`
- `grep 'POSEIDON_API_KEY=' .env.example` matches
- `grep 'FINMIND_TOKEN=' .env.example` matches
</acceptance_criteria>
</task>

<task id="1.6">
<title>Create core config module with pydantic-settings</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/triton/src/triton/config.py
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/.planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md (section 6: API Key Auth Middleware, section 7: Project Structure)
</read_first>
<action>
Create the following files:

1. `src/poseidon/__init__.py` -- empty file
2. `src/poseidon/core/__init__.py` -- empty file
3. `src/poseidon/core/config.py` with:

```python
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    database_url: str = "postgresql://poseidon:poseidon@localhost:5432/poseidon"
    redis_url: str = "redis://localhost:6379/0"
    api_host: str = "0.0.0.0"
    api_port: int = 8000
    api_key: str = ""
    finmind_token: str = ""
    symbols_config: str = "config/symbols.yaml"

    model_config = {"env_prefix": "POSEIDON_", "env_file": ".env"}


settings = Settings()
```

The `env_prefix = "POSEIDON_"` means all env vars must start with `POSEIDON_` (e.g., `POSEIDON_DATABASE_URL`, `POSEIDON_API_KEY`). The exception is `FINMIND_TOKEN` which is read directly by the FinMind fetcher from the environment, or passed through as `POSEIDON_FINMIND_TOKEN`.
</action>
<acceptance_criteria>
- File exists at `src/poseidon/__init__.py`
- File exists at `src/poseidon/core/__init__.py`
- File exists at `src/poseidon/core/config.py`
- `grep 'class Settings(BaseSettings)' src/poseidon/core/config.py` matches
- `grep '"env_prefix": "POSEIDON_"' src/poseidon/core/config.py` matches
- `grep 'api_key: str' src/poseidon/core/config.py` matches
- `grep 'database_url: str' src/poseidon/core/config.py` matches
- `grep 'redis_url: str' src/poseidon/core/config.py` matches
- `grep 'finmind_token: str' src/poseidon/core/config.py` matches
- `grep 'settings = Settings()' src/poseidon/core/config.py` matches
</acceptance_criteria>
</task>

<task id="1.7">
<title>Create API auth dependency and health endpoint</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/src/poseidon/core/config.py (just created in 1.6)
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/.planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md (section 6: API Key Auth Middleware)
</read_first>
<action>
Create 3 files:

1. `src/poseidon/api/__init__.py` -- empty file

2. `src/poseidon/api/auth.py`:
```python
from fastapi import HTTPException, Security
from fastapi.security import APIKeyHeader

from poseidon.core.config import settings

api_key_header = APIKeyHeader(name="X-API-Key", auto_error=True)


async def verify_api_key(api_key: str = Security(api_key_header)) -> str:
    """Validate X-API-Key header against configured key."""
    if api_key != settings.api_key:
        raise HTTPException(
            status_code=401,
            detail="Invalid API key",
        )
    return api_key
```

3. `src/poseidon/api/health.py`:
```python
from fastapi import APIRouter

router = APIRouter()


@router.get("/health")
async def health():
    """Health check endpoint. No authentication required."""
    return {"status": "ok"}
```
</action>
<acceptance_criteria>
- File exists at `src/poseidon/api/__init__.py`
- File exists at `src/poseidon/api/auth.py`
- File exists at `src/poseidon/api/health.py`
- `grep 'APIKeyHeader(name="X-API-Key"' src/poseidon/api/auth.py` matches
- `grep 'status_code=401' src/poseidon/api/auth.py` matches
- `grep 'async def verify_api_key' src/poseidon/api/auth.py` matches
- `grep '@router.get("/health")' src/poseidon/api/health.py` matches
- `grep '"status": "ok"' src/poseidon/api/health.py` matches
</acceptance_criteria>
</task>

<task id="1.8">
<title>Create FastAPI app entry point (main.py)</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/triton/src/triton/main.py
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/src/poseidon/api/auth.py (just created in 1.7)
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/src/poseidon/api/health.py (just created in 1.7)
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/.planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md (section 6: Applying Globally)
</read_first>
<action>
Create `src/poseidon/main.py`:

```python
from fastapi import Depends, FastAPI

from poseidon.api import health
from poseidon.api.auth import verify_api_key

app = FastAPI(title="Poseidon", description="Trading signal platform for OpenClaw")

# Health endpoint: NO authentication (used by Docker healthcheck)
app.include_router(health.router, tags=["health"])

# Secured routers are added here as they are implemented in later plans.
# Pattern: app.include_router(router, prefix="/path", tags=["tag"], dependencies=[Depends(verify_api_key)])
```

Note: The sentiment and data routers will be added by Plan 5 (Wave 3). The `dependencies=[Depends(verify_api_key)]` pattern is applied per-router, NOT as a global app dependency. This keeps `/health` exempt from auth.
</action>
<acceptance_criteria>
- File exists at `src/poseidon/main.py`
- `grep 'FastAPI(title="Poseidon"' src/poseidon/main.py` matches
- `grep 'from poseidon.api import health' src/poseidon/main.py` matches
- `grep 'from poseidon.api.auth import verify_api_key' src/poseidon/main.py` matches
- `grep 'app.include_router(health.router' src/poseidon/main.py` matches
</acceptance_criteria>
</task>

<task id="1.9">
<title>Create Alembic configuration and env.py</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/triton/alembic/env.py
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/src/poseidon/core/config.py (just created in 1.6)
</read_first>
<action>
Create 3 files:

1. `alembic.ini` at project root:
```ini
[alembic]
script_location = alembic
prepend_sys_path = src
sqlalchemy.url = postgresql://poseidon:poseidon@localhost:5432/poseidon

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
```

2. `alembic/env.py`:
```python
from logging.config import fileConfig

from alembic import context
from sqlalchemy import engine_from_config, pool

from poseidon.core.config import settings

config = context.config
config.set_main_option("sqlalchemy.url", settings.database_url)

if config.config_file_name is not None:
    fileConfig(config.config_file_name)

# Import all models so Alembic can detect them for autogenerate.
# Models are added by Plan 2.
target_metadata = None


def run_migrations_offline():
    url = config.get_main_option("sqlalchemy.url")
    context.configure(url=url, target_metadata=target_metadata, literal_binds=True)
    with context.begin_transaction():
        context.run_migrations()


def run_migrations_online():
    connectable = engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )
    with connectable.connect() as connection:
        context.configure(connection=connection, target_metadata=target_metadata)
        with context.begin_transaction():
            context.run_migrations()


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```

3. `alembic/versions/.gitkeep` -- empty file to ensure the directory exists in version control.
</action>
<acceptance_criteria>
- File exists at `poseidon/alembic.ini`
- File exists at `poseidon/alembic/env.py`
- File exists at `poseidon/alembic/versions/.gitkeep`
- `grep 'prepend_sys_path = src' alembic.ini` matches
- `grep 'script_location = alembic' alembic.ini` matches
- `grep 'from poseidon.core.config import settings' alembic/env.py` matches
- `grep 'config.set_main_option("sqlalchemy.url", settings.database_url)' alembic/env.py` matches
</acceptance_criteria>
</task>

<task id="1.10">
<title>Create empty module packages, symbol config, and test scaffold</title>
<read_first>
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/.planning/phases/01-infrastructure-data-foundation/01-RESEARCH.md (section 7: Source Layout, Symbol Watchlist Configuration)
- /Users/jordanchen/Workspace/Projects/aquarium/poseidon/.planning/phases/01-infrastructure-data-foundation/01-CONTEXT.md (Symbol watchlist scope decisions)
</read_first>
<action>
Create all remaining empty package `__init__.py` files and the symbol config seed.

Empty `__init__.py` files (all contain just an empty file or single-line comment):
- `src/poseidon/data/__init__.py`
- `src/poseidon/data/fetchers/__init__.py`
- `src/poseidon/models/__init__.py`
- `src/poseidon/workers/__init__.py`
- `src/poseidon/strategies/__init__.py`
- `src/poseidon/signals/__init__.py`
- `src/poseidon/backtest/__init__.py`
- `src/poseidon/risk/__init__.py`
- `tests/__init__.py`

Create `config/symbols.yaml` with 20+ symbols for equity/crypto markets. Note: tw_futures is an exception -- the Taiwan futures market has a limited number of distinct contracts, so we include all available major contracts (>= 4) rather than forcing 20+:

```yaml
# Poseidon Symbol Watchlist
# Seed configuration. Thalassa can add/remove symbols via API.

markets:
  tw_stock:
    instrument: spot
    intervals: ["1d"]
    symbols:
      - { id: "2330", name: "TSMC" }
      - { id: "2317", name: "Hon Hai" }
      - { id: "2454", name: "MediaTek" }
      - { id: "2308", name: "Delta Electronics" }
      - { id: "2881", name: "Fubon FHC" }
      - { id: "2882", name: "Cathay FHC" }
      - { id: "2891", name: "CTBC FHC" }
      - { id: "2303", name: "UMC" }
      - { id: "1301", name: "Formosa Plastics" }
      - { id: "1303", name: "Nan Ya Plastics" }
      - { id: "2002", name: "China Steel" }
      - { id: "2412", name: "Chunghwa Telecom" }
      - { id: "3711", name: "ASE Technology" }
      - { id: "2886", name: "Mega FHC" }
      - { id: "6505", name: "Formosa Chemicals" }
      - { id: "2892", name: "First FHC" }
      - { id: "3008", name: "LARGAN Precision" }
      - { id: "2382", name: "Quanta Computer" }
      - { id: "2357", name: "ASUS" }
      - { id: "3045", name: "Taiwan Mobile" }
      - { id: "2603", name: "Evergreen Marine" }
      - { id: "2880", name: "Hua Nan FHC" }

  tw_futures:
    instrument: futures
    intervals: ["1d"]
    symbols:
      - { id: "TX", name: "TAIEX Futures" }
      - { id: "MTX", name: "Mini TAIEX Futures" }
      - { id: "TE", name: "Electronics Futures" }
      - { id: "TF", name: "Finance Futures" }

  us_stock:
    instrument: spot
    intervals: ["1d"]
    symbols:
      - { id: "AAPL", name: "Apple" }
      - { id: "MSFT", name: "Microsoft" }
      - { id: "GOOGL", name: "Alphabet" }
      - { id: "AMZN", name: "Amazon" }
      - { id: "NVDA", name: "NVIDIA" }
      - { id: "META", name: "Meta Platforms" }
      - { id: "TSLA", name: "Tesla" }
      - { id: "TSM", name: "TSMC ADR" }
      - { id: "JPM", name: "JPMorgan Chase" }
      - { id: "V", name: "Visa" }
      - { id: "UNH", name: "UnitedHealth" }
      - { id: "JNJ", name: "Johnson & Johnson" }
      - { id: "XOM", name: "ExxonMobil" }
      - { id: "PG", name: "Procter & Gamble" }
      - { id: "MA", name: "Mastercard" }
      - { id: "HD", name: "Home Depot" }
      - { id: "COST", name: "Costco" }
      - { id: "ABBV", name: "AbbVie" }
      - { id: "AMD", name: "AMD" }
      - { id: "CRM", name: "Salesforce" }
      - { id: "NFLX", name: "Netflix" }

  crypto_spot:
    instrument: spot
    intervals: ["1d", "1h"]
    symbols:
      - { id: "BTCUSDT", name: "Bitcoin", ccxt_symbol: "BTC/USDT" }
      - { id: "ETHUSDT", name: "Ethereum", ccxt_symbol: "ETH/USDT" }
      - { id: "SOLUSDT", name: "Solana", ccxt_symbol: "SOL/USDT" }
      - { id: "BNBUSDT", name: "BNB", ccxt_symbol: "BNB/USDT" }
      - { id: "XRPUSDT", name: "XRP", ccxt_symbol: "XRP/USDT" }
      - { id: "ADAUSDT", name: "Cardano", ccxt_symbol: "ADA/USDT" }
      - { id: "DOGEUSDT", name: "Dogecoin", ccxt_symbol: "DOGE/USDT" }
      - { id: "AVAXUSDT", name: "Avalanche", ccxt_symbol: "AVAX/USDT" }
      - { id: "DOTUSDT", name: "Polkadot", ccxt_symbol: "DOT/USDT" }
      - { id: "LINKUSDT", name: "Chainlink", ccxt_symbol: "LINK/USDT" }
```

Create `tests/conftest.py`:
```python
import pytest


@pytest.fixture(autouse=True)
def test_settings(monkeypatch):
    """Override settings for tests."""
    monkeypatch.setenv("POSEIDON_API_KEY", "test-key")
    monkeypatch.setenv("POSEIDON_DATABASE_URL", "postgresql://test:test@localhost:5432/test_poseidon")
    monkeypatch.setenv("POSEIDON_REDIS_URL", "redis://localhost:6379/1")
```
</action>
<acceptance_criteria>
- `ls src/poseidon/data/__init__.py` succeeds
- `ls src/poseidon/data/fetchers/__init__.py` succeeds
- `ls src/poseidon/models/__init__.py` succeeds
- `ls src/poseidon/workers/__init__.py` succeeds
- `ls src/poseidon/strategies/__init__.py` succeeds
- `ls src/poseidon/signals/__init__.py` succeeds
- `ls src/poseidon/backtest/__init__.py` succeeds
- `ls src/poseidon/risk/__init__.py` succeeds
- `ls tests/__init__.py` succeeds
- File exists at `config/symbols.yaml`
- `grep 'id: "2330"' config/symbols.yaml` matches
- `grep 'ccxt_symbol: "BTC/USDT"' config/symbols.yaml` matches
- `grep -c 'id:' config/symbols.yaml` returns at least 57 (22 tw_stock + 4 tw_futures + 21 us_stock + 10 crypto_spot). Per-market minimum counts: tw_stock >= 20, us_stock >= 20, crypto_spot >= 10, tw_futures >= 4 (exception: limited contract universe)
- File exists at `tests/conftest.py`
- `grep 'POSEIDON_API_KEY' tests/conftest.py` matches
</acceptance_criteria>
</task>

</tasks>

<verification>
Run the following commands from the `poseidon/` project root to verify Plan 1 is complete:

```bash
# 1. Verify project structure exists
ls pyproject.toml Dockerfile Dockerfile.gpu docker-compose.yml .env.example alembic.ini
ls alembic/env.py alembic/versions/.gitkeep
ls src/poseidon/__init__.py src/poseidon/main.py
ls src/poseidon/core/config.py src/poseidon/api/auth.py src/poseidon/api/health.py
ls config/symbols.yaml tests/conftest.py

# 2. Verify all 6 docker-compose services
grep -E '^\s{2}\w' docker-compose.yml | head -6

# 3. Verify config env_prefix
grep 'env_prefix' src/poseidon/core/config.py

# 4. Verify auth returns 401
grep 'status_code=401' src/poseidon/api/auth.py

# 5. Verify health endpoint exists
grep '/health' src/poseidon/api/health.py

# 6. Count symbols in watchlist
grep -c 'id:' config/symbols.yaml

# 7. Verify all module packages exist
for mod in data data/fetchers models workers strategies signals backtest risk; do
  ls "src/poseidon/$mod/__init__.py"
done
```
</verification>
