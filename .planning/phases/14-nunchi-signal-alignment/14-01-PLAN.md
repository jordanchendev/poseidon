---
phase: 14-nunchi-signal-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/poseidon/backtest/metrics.py
  - src/poseidon/backtest/portfolio.py
  - src/poseidon/strategies/configs/nunchi_crypto_1h.json
  - tests/test_composite_score.py
  - tests/test_portfolio_short.py
autonomous: true
requirements: [ALIGN-01, ALIGN-02, ALIGN-03]

must_haves:
  truths:
    - "Composite score penalizes drawdown only above 15% threshold, not quadratically from 0%"
    - "Composite score uses capital turnover ratio for turnover penalty, not raw trade count"
    - "Short positions are valued correctly in equity curve (entry_value - unrealized_loss, not quantity * price)"
    - "Nunchi config uses BB threshold 0.85 and ATR multiplier 5.5"
  artifacts:
    - path: "src/poseidon/backtest/metrics.py"
      provides: "Updated compute_composite_score with Nunchi formula"
      contains: "max(0, max_drawdown - 0.15) * 0.05"
    - path: "src/poseidon/backtest/portfolio.py"
      provides: "Correct short position valuation in record_equity_point"
      contains: "entry_price - current_price"
    - path: "src/poseidon/strategies/configs/nunchi_crypto_1h.json"
      provides: "Updated baseline config with Nunchi-proven values"
      contains: "0.85"
    - path: "tests/test_composite_score.py"
      provides: "Tests for new formula behavior"
    - path: "tests/test_portfolio_short.py"
      provides: "Tests for short equity valuation"
  key_links:
    - from: "src/poseidon/backtest/metrics.py"
      to: "BayesianOptimizer, RegimeSearchPipeline, outperformance gate"
      via: "compute_composite_score() is the single optimization metric"
      pattern: "compute_composite_score"
---

<objective>
Fix three independent correctness issues: composite score formula alignment with Nunchi, short position equity valuation, and Nunchi config default values (ATR multiplier, BB threshold).

Purpose: These are the "value fixes" that directly caused 0% autoresearch pass rate -- the scoring formula was too harsh, shorts were mispriced, and default parameters were wrong.
Output: Corrected metrics.py, portfolio.py, nunchi_crypto_1h.json with tests.
</objective>

<execution_context>
@/Users/jordanchen/Workspace/Projects/aquarium/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jordanchen/Workspace/Projects/aquarium/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-nunchi-signal-alignment/14-CONTEXT.md

@src/poseidon/backtest/metrics.py
@src/poseidon/backtest/portfolio.py
@src/poseidon/strategies/configs/nunchi_crypto_1h.json
@tests/test_composite_score.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix composite score formula and update tests</name>
  <files>src/poseidon/backtest/metrics.py, tests/test_composite_score.py</files>
  <read_first>
    - src/poseidon/backtest/metrics.py (current compute_composite_score at lines 89-113)
    - tests/test_composite_score.py (existing test cases to preserve/update)
    - .planning/phases/14-nunchi-signal-alignment/14-CONTEXT.md (D-15 through D-18 for exact formula)
  </read_first>
  <behavior>
    - Test: metrics with max_drawdown=0.10, trade_count=60, sharpe=2.0 -> dd_penalty=0.0 (under 15% threshold)
    - Test: metrics with max_drawdown=0.30, trade_count=60, sharpe=2.0 -> dd_penalty=max(0, 0.30-0.15)*0.05 = 0.0075
    - Test: metrics with trade_count=100, avg_trade_value=1000, capital=100000 -> turnover_ratio=1.0, penalty=max(0, 1.0-500)*0.001 = 0.0 (under 500 threshold)
    - Test: metrics with trade_count=1000, avg_trade_value=1000, capital=100000 -> turnover_ratio=10.0, penalty=max(0, 10.0-500)*0.001 = 0.0 (still under -- need higher to trigger)
    - Test: hard cutoffs unchanged: trade_count<10 -> 0.0, max_drawdown>0.50 -> 0.0, total_return<-0.50 -> 0.0
  </behavior>
  <action>
    In `src/poseidon/backtest/metrics.py`, function `compute_composite_score()`:

    1. Change dd_penalty calculation (line 110) from:
       `dd_penalty = max_drawdown ** 2`
       to:
       `dd_penalty = max(0, max_drawdown - 0.15) * 0.05`
       Per D-15: drawdown under 15% incurs zero penalty.

    2. Change turnover_penalty calculation (line 111) from:
       `turnover_penalty = max(0, (trade_count - 200) / 1000.0)`
       to:
       ```python
       avg_trade_value = metrics.get("avg_trade_value", 0.0)
       capital = metrics.get("initial_capital", 100000.0)
       turnover_ratio = (trade_count * avg_trade_value / capital) if capital > 0 else 0.0
       turnover_penalty = max(0, turnover_ratio - 500) * 0.001
       ```
       Per D-16: based on capital turnover ratio, not raw trade count.

    3. Update the docstring to reflect the new formula:
       `Formula: sharpe * sqrt(min(trades/50, 1.0)) - dd_penalty - turnover_penalty`
       `dd_penalty = max(0, max_drawdown - 0.15) * 0.05`
       `turnover_penalty = max(0, turnover_ratio - 500) * 0.001 where turnover_ratio = trade_count * avg_trade_value / capital`

    4. Hard cutoffs (line 106) remain unchanged per D-17.

    In `tests/test_composite_score.py`:
    - Update existing tests that assert old dd_penalty/turnover_penalty values
    - Add test for dd under 15% -> zero penalty
    - Add test for dd at 30% -> penalty = 0.0075
    - Add test for turnover ratio calculation
    - Preserve all hard cutoff tests
  </action>
  <verify>
    <automated>cd /Users/jordanchen/Workspace/Projects/aquarium/poseidon && python -m pytest tests/test_composite_score.py -x -v 2>&1 | head -60</automated>
  </verify>
  <acceptance_criteria>
    - metrics.py contains `max(0, max_drawdown - 0.15) * 0.05` (grep-verifiable)
    - metrics.py contains `avg_trade_value` and `initial_capital` (grep-verifiable)
    - metrics.py contains `turnover_ratio - 500` (grep-verifiable)
    - metrics.py does NOT contain `max_drawdown ** 2` (grep-verifiable)
    - metrics.py does NOT contain `(trade_count - 200) / 1000` (grep-verifiable)
    - tests/test_composite_score.py contains test for dd below 15% threshold
    - All tests in tests/test_composite_score.py pass
  </acceptance_criteria>
  <done>Composite score formula matches Nunchi: lenient drawdown penalty (15% threshold), capital turnover ratio penalty, unchanged hard cutoffs. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix short position equity valuation and update Nunchi config defaults</name>
  <files>src/poseidon/backtest/portfolio.py, src/poseidon/strategies/configs/nunchi_crypto_1h.json, tests/test_portfolio_short.py</files>
  <read_first>
    - src/poseidon/backtest/portfolio.py (record_equity_point at line 268, _execute_short at line 190)
    - src/poseidon/strategies/configs/nunchi_crypto_1h.json (current config)
    - .planning/phases/14-nunchi-signal-alignment/14-CONTEXT.md (D-12, D-13, D-14, D-05)
  </read_first>
  <behavior>
    - Test: short position opened at entry_price=100, current_price=90 -> position_value = (100-90)*qty (profit of 10 per unit)
    - Test: short position opened at entry_price=100, current_price=110 -> position_value = (100-110)*qty (loss of 10 per unit, negative)
    - Test: equity = cash + sum(position_values) where short position_value accounts for direction
    - Test: long position equity unchanged: quantity * current_price
  </behavior>
  <action>
    1. In `src/poseidon/backtest/portfolio.py`, method `record_equity_point()` (line 268):

    Change position_value calculation from:
    ```python
    position_value = sum(
        pos["quantity"] * current_price for pos in self.positions.values()
    )
    ```
    to:
    ```python
    position_value = 0.0
    for pos in self.positions.values():
        if pos.get("side") == "short":
            # Short: value = entry_value - unrealized_loss = (entry_price - current_price) * quantity
            position_value += (pos["entry_price"] - current_price) * pos["quantity"]
        else:
            # Long: value = quantity * current_price
            position_value += pos["quantity"] * current_price
    ```
    Per D-12: short positions must use `entry_value - (current_price - entry_price) * quantity`.

    2. In `src/poseidon/strategies/configs/nunchi_crypto_1h.json`:
    - Change BB squeeze threshold from `0.2` to `0.85` (per D-14)
    - Add top-level key `"atr_multiplier": 5.5` (per D-05, VotingStrategyFactory.from_config reads this)

    3. Create `tests/test_portfolio_short.py` with tests for:
    - Short position equity calculation when price drops (profitable short)
    - Short position equity calculation when price rises (losing short)
    - Mixed long+short positions equity calculation
    - Long-only position equity unchanged from before
  </action>
  <verify>
    <automated>cd /Users/jordanchen/Workspace/Projects/aquarium/poseidon && python -m pytest tests/test_portfolio_short.py -x -v 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - portfolio.py record_equity_point contains `pos.get("side") == "short"` (grep-verifiable)
    - portfolio.py record_equity_point contains `pos["entry_price"] - current_price` (grep-verifiable)
    - nunchi_crypto_1h.json contains `"threshold": 0.85` for BB squeeze (grep-verifiable)
    - nunchi_crypto_1h.json contains `"atr_multiplier": 5.5` (grep-verifiable)
    - nunchi_crypto_1h.json does NOT contain `"threshold": 0.2` (grep-verifiable)
    - tests/test_portfolio_short.py exists and passes
  </acceptance_criteria>
  <done>Short positions valued correctly in equity curve. Nunchi config updated with ATR 5.5 and BB 0.85. Tests prove both long and short equity calculations are correct.</done>
</task>

</tasks>

<verification>
- `grep -n "max_drawdown - 0.15" src/poseidon/backtest/metrics.py` returns a match
- `grep -n "turnover_ratio" src/poseidon/backtest/metrics.py` returns a match
- `grep -n "entry_price.*current_price" src/poseidon/backtest/portfolio.py` returns a match near record_equity_point
- `grep "0.85" src/poseidon/strategies/configs/nunchi_crypto_1h.json` returns a match
- `grep "5.5" src/poseidon/strategies/configs/nunchi_crypto_1h.json` returns a match
- `python -m pytest tests/test_composite_score.py tests/test_portfolio_short.py -x` passes
</verification>

<success_criteria>
- Composite score formula uses lenient drawdown penalty (15% threshold) and capital turnover ratio penalty
- Short positions valued correctly in equity curve
- Nunchi config has ATR multiplier 5.5 and BB threshold 0.85
- All new and updated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-nunchi-signal-alignment/14-01-SUMMARY.md`
</output>
