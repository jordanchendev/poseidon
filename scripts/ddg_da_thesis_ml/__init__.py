"""Phase 92 thesis ML re-implementations.

Wave 2 deliverable: re-implements rule-based v18-era thesis drivers as
qlib Alpha158 + LGBModel pipelines so DDG-DA's `model.fit()` wrapping
contract can attach. Each module exports a `run()` entry point.
"""
